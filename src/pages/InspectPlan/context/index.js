import React, {createContext} from 'react';

export const InspectPlanContext = createContext({});
export const InspectFormContext = createContext({});
export const InspectPartyContext = createContext({});
export const InspectDetailContext = createContext({});